import com.enniovisco.dsl.*

// We are targeting the Pixel 5 resolution
WebSource.screenWidth = 375 // px
WebSource.screenHeight = 667 // px

WebSource.browser = Browser.CHROME

WebSource.wait = 0

WebSource.maxSessionDuration = 55_000 // ms

WebSource.targetUrl = "https://dl.acm.org/action/showLogin"

